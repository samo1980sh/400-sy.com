# Front product code + color code display

This frontend-only update displays product code with selected color code, e.g. S6R150-A1, and hides the standalone color-code line from product details, quick view, and quick add. No database, admin, or import logic was changed.
